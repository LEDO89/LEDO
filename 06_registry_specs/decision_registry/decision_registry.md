# **Decisionregistry**

## **1\. Overview**

`decision_registry` is a core registry in the LEDO Ontology-Centric Cyber-Physical System. It defines and governs Decision Types, Decision Case Types, Decision Criteria, Escalation Rules, Risk Classification, Evidence Evaluation Requirements, Policy Evaluation Requirements, and Approval Routing Requirements.

The purpose of this registry is to prevent an `ActionCandidate` generated by an agent from moving directly into approval or execution stages, and to ensure that every candidate action is reviewed through a registered Decision Rule and Decision Case structure.

`decision_registry` is not a simple list of decisions.

It is an **operational contract registry for judgment procedures and decision flows** that defines the following:

Which DecisionCase should an ActionCandidate be converted into?

Which evidence must be evaluated?

Which policies must be reviewed?

Which risk class should be assigned?

When is escalation required?

Which approval route should be used?

When should the case be automatically rejected or held?

Which lifecycle object may follow the Decision outcome?

In other words, `decision_registry` deterministically controls the judgment flow before an agent proposal can move toward human approval or execution preparation.

---

## **2\. Core Principle**

A Decision is not execution.

A Decision is not approval.

A Decision is not the Safety Gate.

A Decision is not a physical command.

A Decision is not adapter selection.

The role of a Decision is:

Convert an ActionCandidate into an evaluable DecisionCase,

evaluate evidence, policy, risk, priority, and approval requirements,

and route it to the next lifecycle stage.

The core principles of the Decision Registry are:

No valid Decision Rule,

no valid DecisionCase.

No valid DecisionCase,

no ApprovalRequest.

No ApprovalRequest,

no ApprovedAction.

No Safety Gate pass,

no ExecutionRequest.

The Decision Registry does not decide what to physically execute.  
It defines **which judgment procedure must be applied to a proposed Action**.

---

## **3\. Position in the LEDO Architecture**

`decision_registry` sits between the Agent Layer and the Approval / Safety Gate layers.

Agent Output

        ↓

ActionCandidate

        ↓

action\_registry validation

        ↓

decision\_registry lookup

        ↓

DecisionCase

        ↓

Evidence Evaluation

        ↓

Policy Evaluation

        ↓

Risk / Priority / Escalation Classification

        ↓

Approval Routing

        ↓

ApprovalRequest

        ↓

ApprovedAction

        ↓

Safety Gate

        ↓

ExecutionRequest

In other words, `decision_registry` controls the point where an ActionCandidate becomes an official judgment case.

---

## **4\. Purpose**

The purpose of `decision_registry` is to ensure the following:

1. Define rules for converting ActionCandidate into DecisionCase  
2. Define Decision Types and Decision Case Types  
3. Define judgment criteria by Action Type  
4. Define Risk Class and Priority criteria  
5. Define evidence evaluation requirements  
6. Define policy evaluation requirements  
7. Define escalation rules  
8. Define approval routing rules  
9. Define conditions for auto-approval, hold, reject, or escalation  
10. Define lifecycle paths available to Decision outcomes  
11. Define audit rules for Decision results  
12. Prevent agents or LLMs from arbitrarily creating DecisionCases  
13. Prevent ApprovalRequest creation without a valid Decision

---

## **5\. Core Distinctions**

### **5.1 Decision Type**

`Decision Type` is a controlled judgment type allowed in the system.

Examples:

safety\_risk\_decision

robot\_dispatch\_decision

inspection\_decision

equipment\_status\_decision

zone\_access\_decision

compliance\_decision

resource\_allocation\_decision

workflow\_routing\_decision

emergency\_escalation\_decision

Decision Type defines what kinds of judgment procedures may exist.

---

### **5.2 Decision Rule**

`Decision Rule` defines how a specific ActionCandidate should be evaluated, which DecisionCase it should become, and which next lifecycle stage it should be routed to.

Examples:

A STOP\_WORK candidate follows the safety\_risk\_decision rule.

A DISPATCH\_ROBOT candidate follows the robot\_dispatch\_decision rule.

A REQUEST\_INSPECTION candidate follows the inspection\_decision rule.

---

### **5.3 DecisionCase**

`DecisionCase` is the official judgment object created for an ActionCandidate.

A DecisionCase includes the following:

action\_candidate\_id

action\_type\_id

decision\_rule\_id

target\_scope

evidence\_bundle\_refs

policy\_evaluation\_refs

risk\_class

priority\_level

escalation\_level

recommended\_approval\_level

decision\_outcome

DecisionCase is a core lifecycle object before approval.

---

### **5.4 Decision Criteria**

`Decision Criteria` are the criteria used for judgment.

Examples:

risk\_score

confidence\_score

evidence\_completeness

policy\_pass\_status

world\_state\_freshness

target\_scope\_validity

human\_presence\_risk

equipment\_status

robot\_availability

zone\_accessibility

Decision Criteria must be explicitly defined.

---

### **5.5 Decision Outcome**

`Decision Outcome` is the judgment result of a DecisionCase.

Recommended outcomes:

proceed\_to\_approval

auto\_reject

hold\_for\_more\_evidence

escalate

defer

merge\_with\_existing\_case

requires\_manual\_review

requires\_runtime\_revalidation

blocked

Decision Outcome is not execution.  
Decision Outcome is the routing result to the next lifecycle stage.

---

## **6\. Scope**

`decision_registry` controls the following fields:

decision\_rule\_id: string

canonical\_name: string

display\_name: string

description: string

semantic\_iri: string

decision\_type: string

decision\_category: string

version: string

status: draft | active | deprecated | migration\_required | retired | blocked

applicable\_action\_type\_refs:

  \- string

applicable\_agent\_type\_refs:

  \- string

applicable\_risk\_classes:

  \- string

applicable\_target\_types:

  \- string

required\_evidence\_refs:

  \- string

required\_policy\_refs:

  \- string

required\_runtime\_snapshot\_refs:

  \- string

decision\_criteria\_refs:

  \- string

risk\_classification\_rule\_ref: string

priority\_classification\_rule\_ref: string

escalation\_rule\_ref: string | null

approval\_routing\_rule\_ref: string | null

allowed\_decision\_outcomes:

  \- string

default\_decision\_outcome: string

requires\_human\_review: boolean

requires\_policy\_evaluation: boolean

requires\_evidence\_binding: boolean

requires\_runtime\_snapshot: boolean

decision\_boundary: string

approval\_boundary: string

execution\_boundary: string

safety\_boundary: string

audit\_event\_refs:

  \- string

owner\_module: string

owner\_team: string

source\_document: string

created\_at: datetime

updated\_at: datetime

deprecated\_since: datetime | null

replacement\_decision\_rule\_id: string | null

---

## **7\. Non-Scope**

`decision_registry` does not define the following:

1. Actual approval authority  
2. Actual human approval decision  
3. Physical execution command  
4. PLC / SCADA / Robot command  
5. Adapter instance selection  
6. Final Safety Gate execution validation  
7. Low-level robot motion planning  
8. All site-specific sensor thresholds  
9. Complete policy pass/fail logic  
10. Raw evidence generation algorithm  
11. Full LLM prompt  
12. External system execution logic

These responsibilities belong to the following modules or systems:

approval\_registry

identity\_registry

policy\_registry

evidence\_registry

runtime\_validation\_registry

adapter\_registry

external\_system\_registry

safety\_gate

domain\_module

robot middleware

PLC / SCADA

safety-rated controller

---

## **8\. Decision Category Model**

Decision Types should be classified by category.

Recommended categories:

SAFETY\_DECISION

ROBOT\_DECISION

EQUIPMENT\_DECISION

ZONE\_DECISION

INSPECTION\_DECISION

WORKFLOW\_DECISION

COMPLIANCE\_DECISION

RESOURCE\_DECISION

EMERGENCY\_DECISION

AUDIT\_DECISION

Examples:

decision\_rule\_id: decision:stop\_work\_safety\_risk\_v1

decision\_category: SAFETY\_DECISION

decision\_rule\_id: decision:dispatch\_robot\_v1

decision\_category: ROBOT\_DECISION

decision\_rule\_id: decision:request\_inspection\_v1

decision\_category: INSPECTION\_DECISION

Decision Category is a basis for determining evidence requirements, policy requirements, escalation rules, and approval routes.

---

## **9\. Decision Outcome Model**

Recommended Decision Outcomes are:

proceed\_to\_approval

auto\_reject

hold\_for\_more\_evidence

escalate

defer

merge\_with\_existing\_case

requires\_manual\_review

requires\_runtime\_revalidation

blocked

### **9.1 proceed\_to\_approval**

The DecisionCase may move to the approval stage.

DecisionCase → ApprovalRequest

---

### **9.2 auto\_reject**

The case is automatically rejected because required conditions are not satisfied.

Examples:

Action Type is blocked

Target type mismatch

Required evidence missing

Policy hard-fail

---

### **9.3 hold\_for\_more\_evidence**

The case is held because evidence is insufficient.

DecisionCase remains open, but ApprovalRequest is not created.

---

### **9.4 escalate**

The case is routed to a higher-level judgment path due to high risk, uncertainty, or impact.

DecisionCase → Escalation Queue

---

### **9.5 defer**

Judgment is deferred when current world state is insufficient or a system dependency is unavailable.

---

### **9.6 merge\_with\_existing\_case**

The case is merged when an identical or similar DecisionCase already exists.

---

### **9.7 requires\_manual\_review**

The case must be reviewed by a human because automatic judgment is not sufficient.

---

### **9.8 requires\_runtime\_revalidation**

Additional validation is required because the runtime world state is changing rapidly.

---

### **9.9 blocked**

The case cannot proceed by rule.

Examples:

Action Type blocked

Agent Type unauthorized

Policy hard prohibition

Safety boundary violation

---

## **10\. Registry Entry Schema**

Each Decision Registry entry should follow this structure:

decision\_rule\_id: string

canonical\_name: string

display\_name: string

description: string

semantic\_iri: string

decision\_type: string

decision\_category: string

version: string

status: draft | active | deprecated | migration\_required | retired | blocked

applicable\_action\_type\_refs:

  \- string

applicable\_agent\_type\_refs:

  \- string

applicable\_risk\_classes:

  \- string

applicable\_target\_types:

  \- string

required\_evidence\_refs:

  \- string

required\_policy\_refs:

  \- string

required\_runtime\_snapshot\_refs:

  \- string

decision\_criteria\_refs:

  \- string

risk\_classification\_rule\_ref: string

priority\_classification\_rule\_ref: string

escalation\_rule\_ref: string | null

approval\_routing\_rule\_ref: string | null

allowed\_decision\_outcomes:

  \- string

default\_decision\_outcome: string

requires\_human\_review: boolean

requires\_policy\_evaluation: boolean

requires\_evidence\_binding: boolean

requires\_runtime\_snapshot: boolean

decision\_boundary: string

approval\_boundary: string

execution\_boundary: string

safety\_boundary: string

audit\_event\_refs:

  \- string

owner\_module: string

owner\_team: string

source\_document: string

created\_at: datetime

updated\_at: datetime

deprecated\_since: datetime | null

replacement\_decision\_rule\_id: string | null

---

## **11\. Example Registry Entry: STOP\_WORK Decision**

decision\_rule\_id: decision:stop\_work\_safety\_risk\_v1

canonical\_name: stop\_work\_safety\_risk\_decision

display\_name: Stop Work Safety Risk Decision

description: A safety decision rule that evaluates a STOP\_WORK ActionCandidate and determines whether it should proceed to approval, escalation, rejection, or hold.

semantic\_iri: ledo:StopWorkSafetyRiskDecisionRule

decision\_type: safety\_risk\_decision

decision\_category: SAFETY\_DECISION

version: 1.0.0

status: active

applicable\_action\_type\_refs:

  \- action:STOP\_WORK

applicable\_agent\_type\_refs:

  \- agent:SAFETY\_RISK\_AGENT

  \- agent:SUPERVISOR\_AGENT

applicable\_risk\_classes:

  \- warning

  \- high\_risk

  \- critical

  \- emergency

applicable\_target\_types:

  \- worker\_group

  \- work\_zone

  \- task

  \- operation

required\_evidence\_refs:

  \- evidence:hazard\_detection\_snapshot

  \- evidence:worker\_location\_snapshot

  \- evidence:risk\_assessment\_snapshot

  \- evidence:sensor\_freshness\_snapshot

required\_policy\_refs:

  \- policy:stop\_work\_policy

  \- policy:safety\_escalation\_policy

  \- policy:worker\_safety\_policy

required\_runtime\_snapshot\_refs:

  \- snapshot:world\_state\_snapshot

  \- snapshot:zone\_status\_snapshot

  \- snapshot:worker\_location\_snapshot

decision\_criteria\_refs:

  \- criteria:risk\_score

  \- criteria:evidence\_completeness

  \- criteria:hazard\_severity

  \- criteria:worker\_exposure

  \- criteria:policy\_pass\_status

  \- criteria:sensor\_freshness

risk\_classification\_rule\_ref: risk:stop\_work\_risk\_classification

priority\_classification\_rule\_ref: priority:safety\_priority\_classification

escalation\_rule\_ref: escalation:safety\_escalation\_rule

approval\_routing\_rule\_ref: approval\_route:stop\_work\_safety\_supervisor\_route

allowed\_decision\_outcomes:

  \- proceed\_to\_approval

  \- auto\_reject

  \- hold\_for\_more\_evidence

  \- escalate

  \- requires\_manual\_review

  \- blocked

default\_decision\_outcome: hold\_for\_more\_evidence

requires\_human\_review: true

requires\_policy\_evaluation: true

requires\_evidence\_binding: true

requires\_runtime\_snapshot: true

decision\_boundary: may\_route\_to\_approval\_or\_escalation\_only

approval\_boundary: does\_not\_grant\_approval

execution\_boundary: does\_not\_create\_execution\_request

safety\_boundary: safety\_gate\_must\_still\_pass\_after\_approval

audit\_event\_refs:

  \- audit:decision\_case\_created

  \- audit:decision\_evidence\_evaluated

  \- audit:decision\_policy\_evaluated

  \- audit:decision\_outcome\_selected

  \- audit:decision\_escalated

owner\_module: safety\_domain\_module

owner\_team: LEDO Safety Governance

source\_document: safety\_decision\_contract\_v1

created\_at: 2026-06-26T00:00:00Z

updated\_at: 2026-06-26T00:00:00Z

deprecated\_since: null

replacement\_decision\_rule\_id: null

---

## **12\. Example Registry Entry: DISPATCH\_ROBOT Decision**

decision\_rule\_id: decision:dispatch\_robot\_v1

canonical\_name: dispatch\_robot\_decision

display\_name: Dispatch Robot Decision

description: A robot decision rule that evaluates a DISPATCH\_ROBOT ActionCandidate and determines approval routing, hold, rejection, or escalation.

semantic\_iri: ledo:DispatchRobotDecisionRule

decision\_type: robot\_dispatch\_decision

decision\_category: ROBOT\_DECISION

version: 1.0.0

status: active

applicable\_action\_type\_refs:

  \- action:DISPATCH\_ROBOT

applicable\_agent\_type\_refs:

  \- agent:ROBOT\_DISPATCH\_AGENT

  \- agent:SUPERVISOR\_AGENT

applicable\_risk\_classes:

  \- notice

  \- warning

  \- high\_risk

applicable\_target\_types:

  \- robot

  \- robot\_fleet

  \- task\_location

  \- work\_zone

required\_evidence\_refs:

  \- evidence:robot\_availability\_snapshot

  \- evidence:zone\_accessibility\_snapshot

  \- evidence:worker\_proximity\_snapshot

  \- evidence:mission\_context\_snapshot

required\_policy\_refs:

  \- policy:robot\_dispatch\_policy

  \- policy:worker\_proximity\_policy

  \- policy:zone\_access\_policy

  \- policy:robot\_safety\_policy

required\_runtime\_snapshot\_refs:

  \- snapshot:robot\_status\_snapshot

  \- snapshot:zone\_status\_snapshot

  \- snapshot:worker\_location\_snapshot

  \- snapshot:fleet\_manager\_status\_snapshot

decision\_criteria\_refs:

  \- criteria:robot\_availability

  \- criteria:zone\_accessibility

  \- criteria:worker\_proximity

  \- criteria:mission\_validity

  \- criteria:fleet\_manager\_reachability

  \- criteria:policy\_pass\_status

risk\_classification\_rule\_ref: risk:robot\_dispatch\_risk\_classification

priority\_classification\_rule\_ref: priority:robot\_task\_priority\_classification

escalation\_rule\_ref: escalation:robot\_dispatch\_escalation\_rule

approval\_routing\_rule\_ref: approval\_route:dispatch\_robot\_supervisor\_route

allowed\_decision\_outcomes:

  \- proceed\_to\_approval

  \- auto\_reject

  \- hold\_for\_more\_evidence

  \- escalate

  \- defer

  \- requires\_runtime\_revalidation

  \- blocked

default\_decision\_outcome: hold\_for\_more\_evidence

requires\_human\_review: true

requires\_policy\_evaluation: true

requires\_evidence\_binding: true

requires\_runtime\_snapshot: true

decision\_boundary: may\_route\_robot\_intent\_to\_approval\_only

approval\_boundary: does\_not\_grant\_robot\_approval

execution\_boundary: does\_not\_create\_robot\_execution\_request

safety\_boundary: robot\_safety\_gate\_and\_fleet\_manager\_validation\_required

audit\_event\_refs:

  \- audit:decision\_case\_created

  \- audit:robot\_decision\_evidence\_evaluated

  \- audit:robot\_decision\_policy\_evaluated

  \- audit:robot\_decision\_outcome\_selected

  \- audit:robot\_decision\_escalated

owner\_module: robot\_domain\_module

owner\_team: LEDO Robotics Integration

source\_document: robot\_decision\_contract\_v1

created\_at: 2026-06-26T00:00:00Z

updated\_at: 2026-06-26T00:00:00Z

deprecated\_since: null

replacement\_decision\_rule\_id: null

---

## **13\. Decision Lifecycle Alignment**

Decision is connected to the following lifecycle objects:

AgentOutput

    ↓

ActionCandidate

    ↓

DecisionRuleLookup

    ↓

DecisionCase

    ↓

EvidenceEvaluation

    ↓

PolicyEvaluation

    ↓

RiskClassification

    ↓

PriorityClassification

    ↓

EscalationDecision

    ↓

ApprovalRouting

    ↓

ApprovalRequest

    ↓

ApprovalDecision

    ↓

ApprovedAction

    ↓

SafetyGateResult

    ↓

ExecutionRequest

DecisionCase is the official judgment object before ApprovalRequest.

DecisionCase must not directly become approval or execution.  
It must pass through `approval_registry` and the approval lifecycle.

---

## **14\. Validation Rules**

A Decision Rule is valid only when the following conditions are satisfied:

1. `decision_rule_id` exists in the registry.  
2. Its status is `active`.  
3. Applicable Action Types are declared.  
4. Applicable Agent Types are declared.  
5. Applicable target types are declared.  
6. Required evidence references are declared.  
7. Required policy references are declared.  
8. Required runtime snapshot references are declared.  
9. Decision criteria are declared.  
10. Risk classification rule is declared.  
11. Priority classification rule is declared.  
12. Allowed decision outcomes are declared.  
13. Default decision outcome is declared.  
14. Decision boundary is declared.  
15. Approval boundary is declared.  
16. Execution boundary is declared.  
17. Safety boundary is declared.  
18. Audit event references are declared.  
19. Owner module is declared.  
20. Version is valid.  
21. If deprecated, migration metadata exists.

If any of these conditions are missing, the Decision Rule must not be used in the operational lifecycle.

---

## **15\. DecisionCase Creation Validation**

A DecisionCase may be created only when the following conditions are satisfied:

Is the ActionCandidate valid?

Does the Action Type exist in action\_registry with active status?

Is the Agent Type that created the ActionCandidate authorized to propose this Action Type?

Is the Decision Rule applicable to this Action Type?

Is the Target Type included in applicable target types?

Does required Evidence exist or is it explicitly marked as pending?

Are required Policy references connected?

Does required Runtime Snapshot exist?

Does it comply with the decision boundary?

If these conditions are not satisfied, a DecisionCase must not be created.

---

## **16\. Evidence Evaluation Rule**

Decision Registry declares which evidence is required.

However, it does not generate the evidence itself.

decision\_registry:

    Declares which evidence is required

evidence\_registry:

    Defines evidence types and schemas

evidence\_binder:

    Binds actual evidence to ActionCandidate / DecisionCase

If required evidence is missing, the DecisionCase must result in one of the following outcomes:

hold\_for\_more\_evidence

auto\_reject

requires\_manual\_review

The selected outcome is defined by the Decision Rule.

---

## **17\. Policy Evaluation Rule**

Decision Registry declares which policy evaluations are required.

However, the actual policy pass/fail logic belongs to the Policy Registry or Policy Engine.

decision\_registry:

    Declares which policies are required

policy\_registry:

    Manages policy definitions

policy\_engine:

    Evaluates pass / fail / warning / requires\_review

When a policy hard-fail occurs, the typical outcomes are:

auto\_reject

blocked

escalate

---

## **18\. Risk Classification Rule**

DecisionCase must have a risk class.

Risk Classification may consider the following factors:

hazard severity

worker exposure

equipment state

zone condition

robot proximity

sensor confidence

policy warning

historical incident pattern

runtime uncertainty

Recommended risk classes:

routine

notice

warning

high\_risk

critical

emergency

Decision Registry references which risk classification rule should be used.

The actual risk score calculation may be handled by a risk engine or domain module.

---

## **19\. Priority Classification Rule**

DecisionCase may have a priority level.

Recommended priority levels:

low

normal

high

urgent

immediate

Priority affects the following:

queue ordering

escalation speed

notification urgency

approval timeout

operator UI priority

audit severity

---

## **20\. Escalation Rule**

Decision Registry defines when escalation is required.

Example escalation conditions:

risk\_class \>= critical

evidence conflict detected

policy warning unresolved

multiple agents disagree

human proximity risk high

external system unavailable

approval timeout

repeated failed attempts

Example escalation targets:

site\_supervisor

safety\_supervisor

site\_manager

war\_room

emergency\_controller

compliance\_officer

Escalation is not approval.  
Escalation routes the case to a higher judgment or approval path.

---

## **21\. Approval Routing Rule**

Decision Registry defines which approval route should be used when the Decision Outcome is `proceed_to_approval`.

DecisionCase

    ↓

approval\_routing\_rule\_ref

    ↓

ApprovalRequest

    ↓

approval\_registry

Decision Registry does not perform approval directly.

Approval is handled by `approval_registry` and human authority.

---

## **22\. Relationship to Agent Vocabulary Registry**

`agent_vocabulary_registry` controls which Agent Type may propose which Action Type.

`decision_registry` controls which Decision Rule the resulting ActionCandidate must follow.

agent\_vocabulary\_registry:

    Is this agent allowed to propose this ActionCandidate?

decision\_registry:

    Which judgment procedure must this ActionCandidate follow?

Even if an agent has a high confidence score, it must not bypass the Decision Registry.

---

## **23\. Relationship to Action Registry**

`action_registry` controls the existence and operational contract of Action Types.

`decision_registry` controls which judgment procedure an Action Type candidate must follow.

action\_registry:

    Is this Action Type valid?

decision\_registry:

    Which DecisionCase should a candidate of this Action Type become?

An Action Type that does not exist in the Action Registry cannot be converted into a DecisionCase.

---

## **24\. Relationship to Approval Registry**

`decision_registry` may determine the approval route.

However, the approval rule itself is managed by `approval_registry`.

decision\_registry:

    Does this DecisionCase require approval?

    If so, which approval route should it follow?

approval\_registry:

    Who can approve it, under what conditions and scope?

---

## **25\. Relationship to Policy Registry**

Decision Registry requires policy references.

Policy Registry manages policy definitions.

Policy Engine evaluates policy pass/fail.

decision\_registry:

    Declares which policy evaluations are required

policy\_registry:

    Manages policy meaning and rules

policy\_engine:

    Performs actual evaluation

---

## **26\. Relationship to Safety Gate**

Decision Registry does not replace the Safety Gate.

Decision occurs before approval.

The Safety Gate performs final execution validation before the ExecutionRequest.

Decision Registry:

    Evaluates ActionCandidate as a DecisionCase

    and determines approval routing

Safety Gate:

    Finally verifies whether an ApprovedAction

    is safe to prepare for execution now

Core principle:

Decision pass ≠ Approval pass

Approval pass ≠ Safety Gate pass

---

## **27\. Relationship to Adapter Registry**

Decision Registry does not select adapters.

However, a Decision Rule may indicate whether the ActionCandidate may eventually follow an external execution path and whether an adapter compatibility check will be required later.

decision\_registry:

    Determines whether this ActionCandidate may have an external execution path

adapter\_registry:

    Selects the actual adapter instance after the Safety Gate

---

## **28\. Relationship to Ontology**

Every Decision Rule may have a semantic IRI.

Example:

decision\_rule\_id: decision:stop\_work\_safety\_risk\_v1

semantic\_iri: ledo:StopWorkSafetyRiskDecisionRule

In the ontology, it may be defined as follows:

ledo:StopWorkSafetyRiskDecisionRule

    rdf:type ledo:DecisionRule ;

    ledo:appliesToAction ledo:StopWorkAction ;

    ledo:requiresEvidence ledo:HazardDetectionSnapshot ;

    ledo:requiresPolicy ledo:StopWorkPolicy ;

    ledo:routesToApproval ledo:SafetySupervisorApproval .

Ontology provides the semantic foundation of Decision Rules.

Decision Registry manages this foundation in the operational system through version, status, validation, routing, and audit rules.

---

## **29\. Versioning and Migration**

Decision Rules must be versioned.

A version change is required when any of the following changes:

1. Applicable Action Type  
2. Applicable Agent Type  
3. Applicable target type  
4. Required evidence  
5. Required policy reference  
6. Runtime snapshot requirement  
7. Decision criteria  
8. Risk classification rule  
9. Priority classification rule  
10. Escalation rule  
11. Approval routing rule  
12. Allowed decision outcome  
13. Decision / approval / execution / safety boundary

Status values:

draft

active

deprecated

migration\_required

retired

blocked

A deprecated Decision Rule must declare:

deprecated\_since: datetime

replacement\_decision\_rule\_id: string | null

migration\_notes: string

A blocked Decision Rule must not be used to create new DecisionCases.

---

## **30\. Implementation Use**

`decision_registry` is used to generate or validate:

1. `DecisionType` enum  
2. `DecisionCategory` enum  
3. `DecisionOutcome` enum  
4. `DecisionRule` DTO  
5. `DecisionCase` DTO constraints  
6. ActionCandidate → DecisionCase validation  
7. Evidence evaluation requirement lookup  
8. Policy evaluation requirement lookup  
9. Risk classification rule lookup  
10. Priority classification rule lookup  
11. Escalation routing  
12. Approval routing  
13. Audit log expectations  
14. Test case generation  
15. Migration rules

Implementation must not use unregistered Decision Rules.

---

## **31\. Recommended Code Structure**

registries/

    decision\_registry/

        decision\_registry.py

        decision\_registry\_entry.py

        decision\_type.py

        decision\_category.py

        decision\_status.py

        decision\_outcome.py

        decision\_validation.py

        decision\_errors.py

        decision\_loader.py

        decision\_migration.py

    agent\_vocabulary\_registry/

    action\_registry/

    evidence\_registry/

    policy\_registry/

    approval\_registry/

    adapter\_registry/

    runtime\_validation\_registry/

---

## **32\. Minimal Pydantic Model**

from enum import Enum

from pydantic import BaseModel, Field

from typing import Optional

from datetime import datetime

class DecisionStatus(str, Enum):

    DRAFT \= "draft"

    ACTIVE \= "active"

    DEPRECATED \= "deprecated"

    MIGRATION\_REQUIRED \= "migration\_required"

    RETIRED \= "retired"

    BLOCKED \= "blocked"

class DecisionCategory(str, Enum):

    SAFETY\_DECISION \= "safety\_decision"

    ROBOT\_DECISION \= "robot\_decision"

    EQUIPMENT\_DECISION \= "equipment\_decision"

    ZONE\_DECISION \= "zone\_decision"

    INSPECTION\_DECISION \= "inspection\_decision"

    WORKFLOW\_DECISION \= "workflow\_decision"

    COMPLIANCE\_DECISION \= "compliance\_decision"

    RESOURCE\_DECISION \= "resource\_decision"

    EMERGENCY\_DECISION \= "emergency\_decision"

    AUDIT\_DECISION \= "audit\_decision"

class DecisionOutcome(str, Enum):

    PROCEED\_TO\_APPROVAL \= "proceed\_to\_approval"

    AUTO\_REJECT \= "auto\_reject"

    HOLD\_FOR\_MORE\_EVIDENCE \= "hold\_for\_more\_evidence"

    ESCALATE \= "escalate"

    DEFER \= "defer"

    MERGE\_WITH\_EXISTING\_CASE \= "merge\_with\_existing\_case"

    REQUIRES\_MANUAL\_REVIEW \= "requires\_manual\_review"

    REQUIRES\_RUNTIME\_REVALIDATION \= "requires\_runtime\_revalidation"

    BLOCKED \= "blocked"

class DecisionRegistryEntry(BaseModel):

    decision\_rule\_id: str

    canonical\_name: str

    display\_name: str

    description: str

    semantic\_iri: str

    decision\_type: str

    decision\_category: DecisionCategory

    version: str

    status: DecisionStatus \= DecisionStatus.DRAFT

    applicable\_action\_type\_refs: list\[str\] \= Field(default\_factory=list)

    applicable\_agent\_type\_refs: list\[str\] \= Field(default\_factory=list)

    applicable\_risk\_classes: list\[str\] \= Field(default\_factory=list)

    applicable\_target\_types: list\[str\] \= Field(default\_factory=list)

    required\_evidence\_refs: list\[str\] \= Field(default\_factory=list)

    required\_policy\_refs: list\[str\] \= Field(default\_factory=list)

    required\_runtime\_snapshot\_refs: list\[str\] \= Field(default\_factory=list)

    decision\_criteria\_refs: list\[str\] \= Field(default\_factory=list)

    risk\_classification\_rule\_ref: str

    priority\_classification\_rule\_ref: str

    escalation\_rule\_ref: Optional\[str\] \= None

    approval\_routing\_rule\_ref: Optional\[str\] \= None

    allowed\_decision\_outcomes: list\[DecisionOutcome\] \= Field(default\_factory=list)

    default\_decision\_outcome: DecisionOutcome

    requires\_human\_review: bool \= False

    requires\_policy\_evaluation: bool \= True

    requires\_evidence\_binding: bool \= True

    requires\_runtime\_snapshot: bool \= True

    decision\_boundary: str

    approval\_boundary: str

    execution\_boundary: str

    safety\_boundary: str

    audit\_event\_refs: list\[str\] \= Field(default\_factory=list)

    owner\_module: str

    owner\_team: str

    source\_document: str

    created\_at: datetime

    updated\_at: datetime

    deprecated\_since: Optional\[datetime\] \= None

    replacement\_decision\_rule\_id: Optional\[str\] \= None

---

## **33\. Core Validation Function**

def validate\_decision\_rule\_for\_candidate(

    entry: DecisionRegistryEntry,

    action\_type\_ref: str,

    agent\_type\_ref: str,

    target\_type: str,

) \-\> None:

    if entry.status \!= DecisionStatus.ACTIVE:

        raise InvalidDecisionRuleError(

            f"Decision Rule is not active: {entry.decision\_rule\_id}"

        )

    if action\_type\_ref not in entry.applicable\_action\_type\_refs:

        raise DecisionActionTypeMismatchError(

            f"Action Type '{action\_type\_ref}' is not applicable to "

            f"Decision Rule '{entry.decision\_rule\_id}'"

        )

    if agent\_type\_ref not in entry.applicable\_agent\_type\_refs:

        raise DecisionAgentTypeMismatchError(

            f"Agent Type '{agent\_type\_ref}' is not applicable to "

            f"Decision Rule '{entry.decision\_rule\_id}'"

        )

    if target\_type not in entry.applicable\_target\_types:

        raise DecisionTargetTypeMismatchError(

            f"Target Type '{target\_type}' is not applicable to "

            f"Decision Rule '{entry.decision\_rule\_id}'"

        )

    if not entry.required\_evidence\_refs:

        raise InvalidDecisionRuleError(

            "required\_evidence\_refs must be declared"

        )

    if not entry.required\_policy\_refs:

        raise InvalidDecisionRuleError(

            "required\_policy\_refs must be declared"

        )

    if not entry.decision\_criteria\_refs:

        raise InvalidDecisionRuleError(

            "decision\_criteria\_refs must be declared"

        )

    if not entry.allowed\_decision\_outcomes:

        raise InvalidDecisionRuleError(

            "allowed\_decision\_outcomes must be declared"

        )

    if entry.default\_decision\_outcome not in entry.allowed\_decision\_outcomes:

        raise InvalidDecisionRuleError(

            "default\_decision\_outcome must be included in allowed\_decision\_outcomes"

        )

    if not entry.decision\_boundary:

        raise InvalidDecisionRuleError(

            "decision\_boundary must be declared"

        )

    if not entry.approval\_boundary:

        raise InvalidDecisionRuleError(

            "approval\_boundary must be declared"

        )

    if not entry.execution\_boundary:

        raise InvalidDecisionRuleError(

            "execution\_boundary must be declared"

        )

    if not entry.safety\_boundary:

        raise InvalidDecisionRuleError(

            "safety\_boundary must be declared"

        )

---

## **34\. Test Scenarios**

Required tests:

1\. Reject unregistered Decision Rule.

2\. Reject inactive Decision Rule.

3\. Reject deprecated Decision Rule.

4\. Reject blocked Decision Rule.

5\. Reject applicable Action Type mismatch.

6\. Reject applicable Agent Type mismatch.

7\. Reject target type mismatch.

8\. Reject missing required evidence.

9\. Reject missing required policy reference.

10\. Reject missing runtime snapshot requirement.

11\. Reject missing decision criteria.

12\. Reject missing allowed outcomes.

13\. Reject default outcome if it is not included in allowed outcomes.

14\. Reject missing decision boundary.

15\. Reject missing approval boundary.

16\. Reject missing execution boundary.

17\. Reject missing safety boundary.

18\. Verify that ApprovalRequest cannot be created without DecisionCase.

19\. Verify that ApprovedAction cannot be created from Decision pass alone.

20\. Verify that ExecutionRequest cannot be created from Decision pass alone.

---

## **35\. Final Rule**

No registered Decision Rule,

no valid DecisionCase.

No valid DecisionCase,

no ApprovalRequest.

If the Decision Outcome is not proceed\_to\_approval,

it cannot move to ApprovalRequest.

Decision is not Approval.

Decision is not the Safety Gate.

Decision is not ExecutionRequest.

Decision is not Physical Command.

Decision is not External System control.

`decision_registry` is a core deterministic judgment registry that controls the space between agent proposals and human approval in the LEDO Action Lifecycle.

This module prevents ActionCandidates from arbitrarily moving into the approval stage and ensures that every candidate action passes through evidence, policy, risk, priority, escalation, and approval routing criteria.

The core definition is:

Decision Registry

\= not a list of decision results,

but an operational contract registry that controls

the judgment criteria, evidence requirements, policy requirements,

risk classification, escalation, and approval routing

required for an ActionCandidate to become a DecisionCase

and move toward ApprovalRequest.

# 

# **decision\_registry 설계 보고서**

## **1\. 개요**

`decision_registry`는 LEDO Ontology-Centric Cyber-Physical System에서 사용되는 Decision Type, Decision Case Type, Decision Criteria, Escalation Rule, Risk Classification, Evidence Evaluation Requirement, Policy Evaluation Requirement, Approval Routing Requirement를 정의하고 통제하는 핵심 레지스트리이다.

이 모듈의 목적은 Agent가 생성한 `ActionCandidate`가 곧바로 approval 또는 execution 단계로 넘어가지 못하도록 막고, 모든 후보 Action이 반드시 등록된 Decision Rule과 Decision Case 구조를 통해 검토되도록 보장하는 것이다.

`decision_registry`는 단순한 의사결정 목록이 아니다.

이 레지스트리는 다음을 정의하는 **판단 절차 및 의사결정 계약 레지스트리**이다.

어떤 ActionCandidate는 어떤 DecisionCase로 전환되어야 하는가?  
어떤 evidence가 평가되어야 하는가?  
어떤 policy가 검토되어야 하는가?  
어떤 risk class로 분류되어야 하는가?  
어떤 경우 escalation이 필요한가?  
어떤 approval route로 보내야 하는가?  
어떤 경우 자동 reject 또는 hold 해야 하는가?  
Decision 결과는 어떤 lifecycle object로 이어질 수 있는가?

즉, `decision_registry`는 Agent의 제안을 인간 승인 또는 실행 준비 단계로 보내기 전에, 판단 흐름을 결정론적으로 통제하는 운영 계약 레지스트리이다.

---

## **2\. 핵심 원칙**

Decision은 실행이 아니다.

Decision은 approval이 아니다.

Decision은 Safety Gate가 아니다.

Decision은 physical command가 아니다.

Decision은 adapter selection이 아니다.

Decision의 역할은 다음과 같다.

ActionCandidate를 평가 가능한 DecisionCase로 만들고,  
증거·정책·위험도·우선순위·승인 필요성을 판단하여  
다음 lifecycle 단계로 라우팅한다.

Decision Registry의 핵심 원칙은 다음과 같다.

No valid Decision Rule,  
no valid DecisionCase.

No valid DecisionCase,  
no ApprovalRequest.

No ApprovalRequest,  
no ApprovedAction.

No Safety Gate pass,  
no ExecutionRequest.

Decision Registry는 “무엇을 실행할지”를 결정하는 모듈이 아니라, **제안된 Action을 어떤 판단 절차로 검토할지 정의하는 모듈**이다.

---

## **3\. LEDO 아키텍처 내 위치**

`decision_registry`는 Agent Layer와 Approval / Safety Gate 사이에 위치한다.

Agent Output  
        ↓  
ActionCandidate  
        ↓  
action\_registry validation  
        ↓  
decision\_registry lookup  
        ↓  
DecisionCase  
        ↓  
Evidence Evaluation  
        ↓  
Policy Evaluation  
        ↓  
Risk / Priority / Escalation Classification  
        ↓  
Approval Routing  
        ↓  
ApprovalRequest  
        ↓  
ApprovedAction  
        ↓  
Safety Gate  
        ↓  
ExecutionRequest

즉, `decision_registry`는 ActionCandidate가 단순 후보에서 공식적인 판단 케이스로 전환되는 지점을 통제한다.

---

## **4\. 목적**

`decision_registry`의 목적은 다음과 같다.

1. ActionCandidate를 DecisionCase로 전환하는 규칙 정의  
2. Decision Type과 Decision Case Type 정의  
3. Action Type별 판단 기준 정의  
4. Risk Class 및 Priority 기준 정의  
5. Evidence evaluation requirement 정의  
6. Policy evaluation requirement 정의  
7. Escalation rule 정의  
8. Approval routing rule 정의  
9. 자동 승인, 보류, 거부, escalation 조건 정의  
10. Decision outcome이 갈 수 있는 lifecycle path 정의  
11. Decision 결과의 audit rule 정의  
12. Agent나 LLM이 임의로 DecisionCase를 생성하지 못하도록 통제  
13. Decision 없는 ApprovalRequest 생성을 방지

---

## **5\. 핵심 구분**

### **5.1 Decision Type**

`Decision Type`은 시스템에서 허용되는 판단 유형이다.

예시:

safety\_risk\_decision  
robot\_dispatch\_decision  
inspection\_decision  
equipment\_status\_decision  
zone\_access\_decision  
compliance\_decision  
resource\_allocation\_decision  
workflow\_routing\_decision  
emergency\_escalation\_decision

Decision Type은 어떤 종류의 판단 절차가 존재할 수 있는지를 정의한다.

---

### **5.2 Decision Rule**

`Decision Rule`은 특정 ActionCandidate를 어떤 기준으로 평가하고, 어떤 DecisionCase로 만들며, 어떤 다음 단계로 라우팅할지 정의하는 규칙이다.

예시:

STOP\_WORK 후보는 safety\_risk\_decision rule을 따른다.  
DISPATCH\_ROBOT 후보는 robot\_dispatch\_decision rule을 따른다.  
REQUEST\_INSPECTION 후보는 inspection\_decision rule을 따른다.

---

### **5.3 DecisionCase**

`DecisionCase`는 하나의 ActionCandidate에 대해 공식적으로 생성된 판단 객체이다.

DecisionCase는 다음을 포함한다.

action\_candidate\_id  
action\_type\_id  
decision\_rule\_id  
target\_scope  
evidence\_bundle\_refs  
policy\_evaluation\_refs  
risk\_class  
priority\_level  
escalation\_level  
recommended\_approval\_level  
decision\_outcome

DecisionCase는 approval 이전 단계의 핵심 객체이다.

---

### **5.4 Decision Criteria**

`Decision Criteria`는 판단에 사용되는 기준이다.

예시:

risk\_score  
confidence\_score  
evidence\_completeness  
policy\_pass\_status  
world\_state\_freshness  
target\_scope\_validity  
human\_presence\_risk  
equipment\_status  
robot\_availability  
zone\_accessibility

Decision Criteria는 반드시 명시적으로 정의되어야 한다.

---

### **5.5 Decision Outcome**

`Decision Outcome`은 DecisionCase의 판단 결과이다.

권장 outcome은 다음과 같다.

proceed\_to\_approval  
auto\_reject  
hold\_for\_more\_evidence  
escalate  
defer  
merge\_with\_existing\_case  
requires\_manual\_review  
requires\_runtime\_revalidation  
blocked

Decision Outcome은 실행이 아니다.  
Decision Outcome은 다음 lifecycle 단계로의 라우팅 결과이다.

---

## **6\. Scope**

`decision_registry`는 다음 항목을 통제한다.

decision\_rule\_id: string  
canonical\_name: string  
display\_name: string  
description: string  
semantic\_iri: string

decision\_type: string  
decision\_category: string

version: string  
status: draft | active | deprecated | migration\_required | retired | blocked

applicable\_action\_type\_refs:  
  \- string

applicable\_agent\_type\_refs:  
  \- string

applicable\_risk\_classes:  
  \- string

applicable\_target\_types:  
  \- string

required\_evidence\_refs:  
  \- string

required\_policy\_refs:  
  \- string

required\_runtime\_snapshot\_refs:  
  \- string

decision\_criteria\_refs:  
  \- string

risk\_classification\_rule\_ref: string  
priority\_classification\_rule\_ref: string  
escalation\_rule\_ref: string | null  
approval\_routing\_rule\_ref: string | null

allowed\_decision\_outcomes:  
  \- string

default\_decision\_outcome: string

requires\_human\_review: boolean  
requires\_policy\_evaluation: boolean  
requires\_evidence\_binding: boolean  
requires\_runtime\_snapshot: boolean

decision\_boundary: string  
approval\_boundary: string  
execution\_boundary: string  
safety\_boundary: string

audit\_event\_refs:  
  \- string

owner\_module: string  
owner\_team: string  
source\_document: string

created\_at: datetime  
updated\_at: datetime  
deprecated\_since: datetime | null  
replacement\_decision\_rule\_id: string | null

---

## **7\. Non-Scope**

`decision_registry`는 다음을 정의하지 않는다.

1. 실제 approval authority  
2. 실제 human approval decision  
3. physical execution command  
4. PLC / SCADA / Robot command  
5. adapter instance selection  
6. Safety Gate 최종 실행 검증  
7. low-level robot motion planning  
8. site-specific sensor threshold 전체  
9. policy pass/fail logic 전체  
10. raw evidence generation algorithm  
11. LLM prompt 전체  
12. external system execution logic

이 책임들은 각각 다음 모듈에 속한다.

approval\_registry  
identity\_registry  
policy\_registry  
evidence\_registry  
runtime\_validation\_registry  
adapter\_registry  
external\_system\_registry  
safety\_gate  
domain\_module  
robot middleware  
PLC / SCADA  
safety-rated controller

---

## **8\. Decision Category 모델**

Decision Type은 category로 분류되어야 한다.

권장 category는 다음과 같다.

SAFETY\_DECISION  
ROBOT\_DECISION  
EQUIPMENT\_DECISION  
ZONE\_DECISION  
INSPECTION\_DECISION  
WORKFLOW\_DECISION  
COMPLIANCE\_DECISION  
RESOURCE\_DECISION  
EMERGENCY\_DECISION  
AUDIT\_DECISION

예시:

decision\_rule\_id: decision:stop\_work\_safety\_risk\_v1  
decision\_category: SAFETY\_DECISION

decision\_rule\_id: decision:dispatch\_robot\_v1  
decision\_category: ROBOT\_DECISION

decision\_rule\_id: decision:request\_inspection\_v1  
decision\_category: INSPECTION\_DECISION

Decision Category는 evidence requirement, policy requirement, escalation rule, approval route를 결정하는 기준이 된다.

---

## **9\. Decision Outcome 모델**

권장 Decision Outcome은 다음과 같다.

proceed\_to\_approval  
auto\_reject  
hold\_for\_more\_evidence  
escalate  
defer  
merge\_with\_existing\_case  
requires\_manual\_review  
requires\_runtime\_revalidation  
blocked

### **9.1 proceed\_to\_approval**

DecisionCase가 approval 단계로 이동할 수 있다.

DecisionCase → ApprovalRequest

---

### **9.2 auto\_reject**

필수 조건이 충족되지 않아 자동 거부된다.

예시:

Action Type이 blocked 상태  
target type 불일치  
필수 evidence 누락  
policy hard-fail

---

### **9.3 hold\_for\_more\_evidence**

증거가 부족하여 보류된다.

DecisionCase는 유지되지만 ApprovalRequest는 생성되지 않는다.

---

### **9.4 escalate**

위험도, 불확실성, 영향도가 높아 상위 판단 경로로 보낸다.

DecisionCase → Escalation Queue

---

### **9.5 defer**

현재 world state가 불충분하거나 system dependency가 unavailable일 때 판단을 연기한다.

---

### **9.6 merge\_with\_existing\_case**

동일 또는 유사한 DecisionCase가 이미 존재하는 경우 병합한다.

---

### **9.7 requires\_manual\_review**

자동 판단이 불가능하여 사람이 검토해야 한다.

---

### **9.8 requires\_runtime\_revalidation**

runtime world state가 빠르게 변하여 추가 검증이 필요하다.

---

### **9.9 blocked**

규칙상 진행할 수 없다.

예시:

Action Type blocked  
Agent Type unauthorized  
Policy hard prohibition  
Safety boundary violation

---

## **10\. Registry Entry Schema**

각 Decision Registry entry는 다음 구조를 따른다.

decision\_rule\_id: string  
canonical\_name: string  
display\_name: string  
description: string  
semantic\_iri: string

decision\_type: string  
decision\_category: string

version: string  
status: draft | active | deprecated | migration\_required | retired | blocked

applicable\_action\_type\_refs:  
  \- string

applicable\_agent\_type\_refs:  
  \- string

applicable\_risk\_classes:  
  \- string

applicable\_target\_types:  
  \- string

required\_evidence\_refs:  
  \- string

required\_policy\_refs:  
  \- string

required\_runtime\_snapshot\_refs:  
  \- string

decision\_criteria\_refs:  
  \- string

risk\_classification\_rule\_ref: string  
priority\_classification\_rule\_ref: string  
escalation\_rule\_ref: string | null  
approval\_routing\_rule\_ref: string | null

allowed\_decision\_outcomes:  
  \- string

default\_decision\_outcome: string

requires\_human\_review: boolean  
requires\_policy\_evaluation: boolean  
requires\_evidence\_binding: boolean  
requires\_runtime\_snapshot: boolean

decision\_boundary: string  
approval\_boundary: string  
execution\_boundary: string  
safety\_boundary: string

audit\_event\_refs:  
  \- string

owner\_module: string  
owner\_team: string  
source\_document: string

created\_at: datetime  
updated\_at: datetime  
deprecated\_since: datetime | null  
replacement\_decision\_rule\_id: string | null

---

## **11\. Registry Entry 예시: STOP\_WORK Decision**

decision\_rule\_id: decision:stop\_work\_safety\_risk\_v1  
canonical\_name: stop\_work\_safety\_risk\_decision  
display\_name: Stop Work Safety Risk Decision  
description: STOP\_WORK ActionCandidate를 평가하여 approval, escalation, reject, hold 여부를 결정하는 safety decision rule이다.  
semantic\_iri: ledo:StopWorkSafetyRiskDecisionRule

decision\_type: safety\_risk\_decision  
decision\_category: SAFETY\_DECISION

version: 1.0.0  
status: active

applicable\_action\_type\_refs:  
  \- action:STOP\_WORK

applicable\_agent\_type\_refs:  
  \- agent:SAFETY\_RISK\_AGENT  
  \- agent:SUPERVISOR\_AGENT

applicable\_risk\_classes:  
  \- warning  
  \- high\_risk  
  \- critical  
  \- emergency

applicable\_target\_types:  
  \- worker\_group  
  \- work\_zone  
  \- task  
  \- operation

required\_evidence\_refs:  
  \- evidence:hazard\_detection\_snapshot  
  \- evidence:worker\_location\_snapshot  
  \- evidence:risk\_assessment\_snapshot  
  \- evidence:sensor\_freshness\_snapshot

required\_policy\_refs:  
  \- policy:stop\_work\_policy  
  \- policy:safety\_escalation\_policy  
  \- policy:worker\_safety\_policy

required\_runtime\_snapshot\_refs:  
  \- snapshot:world\_state\_snapshot  
  \- snapshot:zone\_status\_snapshot  
  \- snapshot:worker\_location\_snapshot

decision\_criteria\_refs:  
  \- criteria:risk\_score  
  \- criteria:evidence\_completeness  
  \- criteria:hazard\_severity  
  \- criteria:worker\_exposure  
  \- criteria:policy\_pass\_status  
  \- criteria:sensor\_freshness

risk\_classification\_rule\_ref: risk:stop\_work\_risk\_classification  
priority\_classification\_rule\_ref: priority:safety\_priority\_classification  
escalation\_rule\_ref: escalation:safety\_escalation\_rule  
approval\_routing\_rule\_ref: approval\_route:stop\_work\_safety\_supervisor\_route

allowed\_decision\_outcomes:  
  \- proceed\_to\_approval  
  \- auto\_reject  
  \- hold\_for\_more\_evidence  
  \- escalate  
  \- requires\_manual\_review  
  \- blocked

default\_decision\_outcome: hold\_for\_more\_evidence

requires\_human\_review: true  
requires\_policy\_evaluation: true  
requires\_evidence\_binding: true  
requires\_runtime\_snapshot: true

decision\_boundary: may\_route\_to\_approval\_or\_escalation\_only  
approval\_boundary: does\_not\_grant\_approval  
execution\_boundary: does\_not\_create\_execution\_request  
safety\_boundary: safety\_gate\_must\_still\_pass\_after\_approval

audit\_event\_refs:  
  \- audit:decision\_case\_created  
  \- audit:decision\_evidence\_evaluated  
  \- audit:decision\_policy\_evaluated  
  \- audit:decision\_outcome\_selected  
  \- audit:decision\_escalated

owner\_module: safety\_domain\_module  
owner\_team: LEDO Safety Governance  
source\_document: safety\_decision\_contract\_v1  
created\_at: 2026-06-26T00:00:00Z  
updated\_at: 2026-06-26T00:00:00Z  
deprecated\_since: null  
replacement\_decision\_rule\_id: null

---

## **12\. Registry Entry 예시: DISPATCH\_ROBOT Decision**

decision\_rule\_id: decision:dispatch\_robot\_v1  
canonical\_name: dispatch\_robot\_decision  
display\_name: Dispatch Robot Decision  
description: DISPATCH\_ROBOT ActionCandidate를 평가하여 approval routing, hold, reject, escalation 여부를 결정하는 robot decision rule이다.  
semantic\_iri: ledo:DispatchRobotDecisionRule

decision\_type: robot\_dispatch\_decision  
decision\_category: ROBOT\_DECISION

version: 1.0.0  
status: active

applicable\_action\_type\_refs:  
  \- action:DISPATCH\_ROBOT

applicable\_agent\_type\_refs:  
  \- agent:ROBOT\_DISPATCH\_AGENT  
  \- agent:SUPERVISOR\_AGENT

applicable\_risk\_classes:  
  \- notice  
  \- warning  
  \- high\_risk

applicable\_target\_types:  
  \- robot  
  \- robot\_fleet  
  \- task\_location  
  \- work\_zone

required\_evidence\_refs:  
  \- evidence:robot\_availability\_snapshot  
  \- evidence:zone\_accessibility\_snapshot  
  \- evidence:worker\_proximity\_snapshot  
  \- evidence:mission\_context\_snapshot

required\_policy\_refs:  
  \- policy:robot\_dispatch\_policy  
  \- policy:worker\_proximity\_policy  
  \- policy:zone\_access\_policy  
  \- policy:robot\_safety\_policy

required\_runtime\_snapshot\_refs:  
  \- snapshot:robot\_status\_snapshot  
  \- snapshot:zone\_status\_snapshot  
  \- snapshot:worker\_location\_snapshot  
  \- snapshot:fleet\_manager\_status\_snapshot

decision\_criteria\_refs:  
  \- criteria:robot\_availability  
  \- criteria:zone\_accessibility  
  \- criteria:worker\_proximity  
  \- criteria:mission\_validity  
  \- criteria:fleet\_manager\_reachability  
  \- criteria:policy\_pass\_status

risk\_classification\_rule\_ref: risk:robot\_dispatch\_risk\_classification  
priority\_classification\_rule\_ref: priority:robot\_task\_priority\_classification  
escalation\_rule\_ref: escalation:robot\_dispatch\_escalation\_rule  
approval\_routing\_rule\_ref: approval\_route:dispatch\_robot\_supervisor\_route

allowed\_decision\_outcomes:  
  \- proceed\_to\_approval  
  \- auto\_reject  
  \- hold\_for\_more\_evidence  
  \- escalate  
  \- defer  
  \- requires\_runtime\_revalidation  
  \- blocked

default\_decision\_outcome: hold\_for\_more\_evidence

requires\_human\_review: true  
requires\_policy\_evaluation: true  
requires\_evidence\_binding: true  
requires\_runtime\_snapshot: true

decision\_boundary: may\_route\_robot\_intent\_to\_approval\_only  
approval\_boundary: does\_not\_grant\_robot\_approval  
execution\_boundary: does\_not\_create\_robot\_execution\_request  
safety\_boundary: robot\_safety\_gate\_and\_fleet\_manager\_validation\_required

audit\_event\_refs:  
  \- audit:decision\_case\_created  
  \- audit:robot\_decision\_evidence\_evaluated  
  \- audit:robot\_decision\_policy\_evaluated  
  \- audit:robot\_decision\_outcome\_selected  
  \- audit:robot\_decision\_escalated

owner\_module: robot\_domain\_module  
owner\_team: LEDO Robotics Integration  
source\_document: robot\_decision\_contract\_v1  
created\_at: 2026-06-26T00:00:00Z  
updated\_at: 2026-06-26T00:00:00Z  
deprecated\_since: null  
replacement\_decision\_rule\_id: null

---

## **13\. Decision Lifecycle Alignment**

Decision은 다음 lifecycle object들과 연결된다.

AgentOutput  
    ↓  
ActionCandidate  
    ↓  
DecisionRuleLookup  
    ↓  
DecisionCase  
    ↓  
EvidenceEvaluation  
    ↓  
PolicyEvaluation  
    ↓  
RiskClassification  
    ↓  
PriorityClassification  
    ↓  
EscalationDecision  
    ↓  
ApprovalRouting  
    ↓  
ApprovalRequest  
    ↓  
ApprovalDecision  
    ↓  
ApprovedAction  
    ↓  
SafetyGateResult  
    ↓  
ExecutionRequest

DecisionCase는 ApprovalRequest 이전의 공식 판단 객체이다.

DecisionCase는 approval 또는 execution으로 직접 변하면 안 된다.  
반드시 approval\_registry와 approval lifecycle을 거쳐야 한다.

---

## **14\. Validation Rules**

Decision Rule은 다음 조건을 만족할 때만 유효하다.

1. `decision_rule_id`가 registry에 존재해야 한다.  
2. status가 `active`이어야 한다.  
3. applicable Action Type이 선언되어야 한다.  
4. applicable Agent Type이 선언되어야 한다.  
5. applicable target type이 선언되어야 한다.  
6. required evidence reference가 선언되어야 한다.  
7. required policy reference가 선언되어야 한다.  
8. required runtime snapshot reference가 선언되어야 한다.  
9. decision criteria가 선언되어야 한다.  
10. risk classification rule이 선언되어야 한다.  
11. priority classification rule이 선언되어야 한다.  
12. allowed decision outcome이 선언되어야 한다.  
13. default decision outcome이 선언되어야 한다.  
14. decision boundary가 선언되어야 한다.  
15. approval boundary가 선언되어야 한다.  
16. execution boundary가 선언되어야 한다.  
17. safety boundary가 선언되어야 한다.  
18. audit event reference가 선언되어야 한다.  
19. owner module이 선언되어야 한다.  
20. version이 유효해야 한다.  
21. deprecated 상태라면 migration metadata가 있어야 한다.

하나라도 누락되면 해당 Decision Rule은 operational lifecycle에 사용되면 안 된다.

---

## **15\. DecisionCase 생성 검증**

DecisionCase는 다음 조건을 만족할 때만 생성될 수 있다.

ActionCandidate가 유효한가?  
Action Type이 action\_registry에 active 상태로 존재하는가?  
ActionCandidate를 생성한 Agent Type이 해당 Action Type을 제안할 권한이 있는가?  
Decision Rule이 해당 Action Type에 적용 가능한가?  
Target Type이 applicable target type에 포함되는가?  
필수 Evidence가 존재하거나 pending으로 명시되어 있는가?  
필수 Policy reference가 연결되어 있는가?  
필수 Runtime Snapshot이 존재하는가?  
Decision boundary를 위반하지 않는가?

이 조건을 통과하지 못하면 DecisionCase는 생성되면 안 된다.

---

## **16\. Evidence Evaluation Rule**

Decision Registry는 어떤 evidence가 필요한지 선언한다.

하지만 evidence 자체를 생성하지는 않는다.

decision\_registry:  
    어떤 evidence가 필요한지 선언

evidence\_registry:  
    evidence type과 schema 정의

evidence\_binder:  
    실제 evidence를 ActionCandidate / DecisionCase에 연결

DecisionCase는 required evidence가 없으면 다음 outcome 중 하나로 처리되어야 한다.

hold\_for\_more\_evidence  
auto\_reject  
requires\_manual\_review

어떤 outcome을 선택할지는 Decision Rule이 정의한다.

---

## **17\. Policy Evaluation Rule**

Decision Registry는 어떤 policy evaluation이 필요한지 선언한다.

하지만 policy pass/fail logic 자체는 Policy Registry 또는 Policy Engine이 담당한다.

decision\_registry:  
    어떤 policy가 필요한지 선언

policy\_registry:  
    policy definition 관리

policy\_engine:  
    pass / fail / warning / requires\_review 판정

Policy hard-fail이 발생하면 일반적으로 다음 outcome을 사용한다.

auto\_reject  
blocked  
escalate

---

## **18\. Risk Classification Rule**

DecisionCase는 risk class를 가져야 한다.

Risk Classification은 다음 요소를 고려할 수 있다.

hazard severity  
worker exposure  
equipment state  
zone condition  
robot proximity  
sensor confidence  
policy warning  
historical incident pattern  
runtime uncertainty

권장 risk class:

routine  
notice  
warning  
high\_risk  
critical  
emergency

Decision Registry는 어떤 risk classification rule을 사용할지 참조한다.

실제 risk score 계산은 risk engine 또는 domain module이 담당할 수 있다.

---

## **19\. Priority Classification Rule**

DecisionCase는 priority level을 가질 수 있다.

권장 priority level:

low  
normal  
high  
urgent  
immediate

Priority는 다음에 영향을 준다.

queue ordering  
escalation speed  
notification urgency  
approval timeout  
operator UI priority  
audit severity

---

## **20\. Escalation Rule**

Decision Registry는 어떤 경우 escalation이 필요한지 정의한다.

Escalation 조건 예시:

risk\_class \>= critical  
evidence conflict detected  
policy warning unresolved  
multiple agents disagree  
human proximity risk high  
external system unavailable  
approval timeout  
repeated failed attempts

Escalation 대상 예시:

site\_supervisor  
safety\_supervisor  
site\_manager  
war\_room  
emergency\_controller  
compliance\_officer

Escalation은 approval이 아니다.  
Escalation은 더 높은 판단 또는 승인 경로로 보내는 것이다.

---

## **21\. Approval Routing Rule**

Decision Registry는 Decision Outcome이 `proceed_to_approval`인 경우 어떤 approval route로 보낼지 정의한다.

DecisionCase  
    ↓  
approval\_routing\_rule\_ref  
    ↓  
ApprovalRequest  
    ↓  
approval\_registry

Decision Registry는 approval을 직접 수행하지 않는다.

Approval은 `approval_registry`와 human authority가 담당한다.

---

## **22\. Relationship to Agent Vocabulary Registry**

`agent_vocabulary_registry`는 어떤 Agent Type이 어떤 Action Type을 제안할 수 있는지 통제한다.

`decision_registry`는 그 ActionCandidate가 어떤 Decision Rule을 따라야 하는지 통제한다.

agent\_vocabulary\_registry:  
    이 Agent가 이 ActionCandidate를 제안할 수 있는가?

decision\_registry:  
    이 ActionCandidate는 어떤 판단 절차를 따라야 하는가?

Agent가 아무리 높은 confidence score를 가져도 Decision Registry를 우회할 수 없다.

---

## **23\. Relationship to Action Registry**

`action_registry`는 Action Type의 존재와 운영 계약을 통제한다.

`decision_registry`는 해당 Action Type이 어떤 판단 절차를 거쳐야 하는지 통제한다.

action\_registry:  
    이 Action Type은 유효한가?

decision\_registry:  
    이 Action Type의 후보는 어떤 DecisionCase로 평가되어야 하는가?

Action Registry에 없는 Action Type은 DecisionCase로 전환될 수 없다.

---

## **24\. Relationship to Approval Registry**

`decision_registry`는 approval route를 결정할 수 있다.

하지만 approval rule 자체는 `approval_registry`가 관리한다.

decision\_registry:  
    이 DecisionCase는 approval이 필요한가?  
    필요하다면 어떤 approval route로 보낼 것인가?

approval\_registry:  
    누가 어떤 조건과 범위에서 승인할 수 있는가?

---

## **25\. Relationship to Policy Registry**

Decision Registry는 policy reference를 요구한다.

Policy Registry는 policy definition을 관리한다.

Policy Engine은 policy pass/fail을 판단한다.

decision\_registry:  
    어떤 policy evaluation이 필요한지 선언

policy\_registry:  
    policy의 의미와 rule 관리

policy\_engine:  
    실제 평가 수행

---

## **26\. Relationship to Safety Gate**

Decision Registry는 Safety Gate를 대체하지 않는다.

Decision은 approval 이전의 판단이다.

Safety Gate는 execution request 이전의 최종 실행 검증이다.

Decision Registry:  
    ActionCandidate를 DecisionCase로 평가하고 approval route를 결정

Safety Gate:  
    ApprovedAction이 지금 실행 준비를 해도 안전한지 최종 검증

핵심 원칙:

Decision pass ≠ Approval pass  
Approval pass ≠ Safety Gate pass

---

## **27\. Relationship to Adapter Registry**

Decision Registry는 adapter를 선택하지 않는다.

다만 특정 Decision Rule은 나중에 external execution 가능성이 있는지, adapter compatibility check가 필요한지 표시할 수 있다.

decision\_registry:  
    이 ActionCandidate가 external execution path를 가질 수 있는지 판단

adapter\_registry:  
    Safety Gate 이후 실제 adapter instance 선택

---

## **28\. Ontology와의 관계**

모든 Decision Rule은 semantic IRI를 가질 수 있다.

예시:

decision\_rule\_id: decision:stop\_work\_safety\_risk\_v1  
semantic\_iri: ledo:StopWorkSafetyRiskDecisionRule

Ontology에서는 다음과 같이 정의할 수 있다.

ledo:StopWorkSafetyRiskDecisionRule  
    rdf:type ledo:DecisionRule ;  
    ledo:appliesToAction ledo:StopWorkAction ;  
    ledo:requiresEvidence ledo:HazardDetectionSnapshot ;  
    ledo:requiresPolicy ledo:StopWorkPolicy ;  
    ledo:routesToApproval ledo:SafetySupervisorApproval .

Ontology는 Decision Rule의 의미론적 기반을 제공한다.

Decision Registry는 이를 운영 시스템에서 version, status, validation, routing, audit rule로 관리한다.

---

## **29\. Versioning 및 Migration**

Decision Rule은 반드시 versioning되어야 한다.

다음 항목 중 하나라도 변경되면 version 변경이 필요하다.

1. applicable Action Type 변경  
2. applicable Agent Type 변경  
3. applicable target type 변경  
4. required evidence 변경  
5. required policy reference 변경  
6. runtime snapshot requirement 변경  
7. decision criteria 변경  
8. risk classification rule 변경  
9. priority classification rule 변경  
10. escalation rule 변경  
11. approval routing rule 변경  
12. allowed decision outcome 변경  
13. decision / approval / execution / safety boundary 변경

Status 값:

draft  
active  
deprecated  
migration\_required  
retired  
blocked

Deprecated Decision Rule은 다음을 선언해야 한다.

deprecated\_since: datetime  
replacement\_decision\_rule\_id: string | null  
migration\_notes: string

Blocked Decision Rule은 새로운 DecisionCase 생성에 사용되면 안 된다.

---

## **30\. Implementation Use**

`decision_registry`는 다음을 생성하거나 검증하는 데 사용된다.

1. `DecisionType` enum  
2. `DecisionCategory` enum  
3. `DecisionOutcome` enum  
4. `DecisionRule` DTO  
5. `DecisionCase` DTO constraints  
6. ActionCandidate → DecisionCase validation  
7. Evidence evaluation requirement lookup  
8. Policy evaluation requirement lookup  
9. Risk classification rule lookup  
10. Priority classification rule lookup  
11. Escalation routing  
12. Approval routing  
13. Audit log expectation  
14. Test case generation  
15. Migration rules

Implementation은 등록되지 않은 Decision Rule을 사용하면 안 된다.

---

## **31\. 권장 Code Structure**

registries/  
    decision\_registry/  
        decision\_registry.py  
        decision\_registry\_entry.py  
        decision\_type.py  
        decision\_category.py  
        decision\_status.py  
        decision\_outcome.py  
        decision\_validation.py  
        decision\_errors.py  
        decision\_loader.py  
        decision\_migration.py

    agent\_vocabulary\_registry/  
    action\_registry/  
    evidence\_registry/  
    policy\_registry/  
    approval\_registry/  
    adapter\_registry/  
    runtime\_validation\_registry/

---

## **32\. Minimal Pydantic Model**

from enum import Enum  
from pydantic import BaseModel, Field  
from typing import Optional  
from datetime import datetime

class DecisionStatus(str, Enum):  
    DRAFT \= "draft"  
    ACTIVE \= "active"  
    DEPRECATED \= "deprecated"  
    MIGRATION\_REQUIRED \= "migration\_required"  
    RETIRED \= "retired"  
    BLOCKED \= "blocked"

class DecisionCategory(str, Enum):  
    SAFETY\_DECISION \= "safety\_decision"  
    ROBOT\_DECISION \= "robot\_decision"  
    EQUIPMENT\_DECISION \= "equipment\_decision"  
    ZONE\_DECISION \= "zone\_decision"  
    INSPECTION\_DECISION \= "inspection\_decision"  
    WORKFLOW\_DECISION \= "workflow\_decision"  
    COMPLIANCE\_DECISION \= "compliance\_decision"  
    RESOURCE\_DECISION \= "resource\_decision"  
    EMERGENCY\_DECISION \= "emergency\_decision"  
    AUDIT\_DECISION \= "audit\_decision"

class DecisionOutcome(str, Enum):  
    PROCEED\_TO\_APPROVAL \= "proceed\_to\_approval"  
    AUTO\_REJECT \= "auto\_reject"  
    HOLD\_FOR\_MORE\_EVIDENCE \= "hold\_for\_more\_evidence"  
    ESCALATE \= "escalate"  
    DEFER \= "defer"  
    MERGE\_WITH\_EXISTING\_CASE \= "merge\_with\_existing\_case"  
    REQUIRES\_MANUAL\_REVIEW \= "requires\_manual\_review"  
    REQUIRES\_RUNTIME\_REVALIDATION \= "requires\_runtime\_revalidation"  
    BLOCKED \= "blocked"

class DecisionRegistryEntry(BaseModel):  
    decision\_rule\_id: str  
    canonical\_name: str  
    display\_name: str  
    description: str  
    semantic\_iri: str

    decision\_type: str  
    decision\_category: DecisionCategory

    version: str  
    status: DecisionStatus \= DecisionStatus.DRAFT

    applicable\_action\_type\_refs: list\[str\] \= Field(default\_factory=list)  
    applicable\_agent\_type\_refs: list\[str\] \= Field(default\_factory=list)  
    applicable\_risk\_classes: list\[str\] \= Field(default\_factory=list)  
    applicable\_target\_types: list\[str\] \= Field(default\_factory=list)

    required\_evidence\_refs: list\[str\] \= Field(default\_factory=list)  
    required\_policy\_refs: list\[str\] \= Field(default\_factory=list)  
    required\_runtime\_snapshot\_refs: list\[str\] \= Field(default\_factory=list)

    decision\_criteria\_refs: list\[str\] \= Field(default\_factory=list)

    risk\_classification\_rule\_ref: str  
    priority\_classification\_rule\_ref: str  
    escalation\_rule\_ref: Optional\[str\] \= None  
    approval\_routing\_rule\_ref: Optional\[str\] \= None

    allowed\_decision\_outcomes: list\[DecisionOutcome\] \= Field(default\_factory=list)  
    default\_decision\_outcome: DecisionOutcome

    requires\_human\_review: bool \= False  
    requires\_policy\_evaluation: bool \= True  
    requires\_evidence\_binding: bool \= True  
    requires\_runtime\_snapshot: bool \= True

    decision\_boundary: str  
    approval\_boundary: str  
    execution\_boundary: str  
    safety\_boundary: str

    audit\_event\_refs: list\[str\] \= Field(default\_factory=list)

    owner\_module: str  
    owner\_team: str  
    source\_document: str

    created\_at: datetime  
    updated\_at: datetime  
    deprecated\_since: Optional\[datetime\] \= None  
    replacement\_decision\_rule\_id: Optional\[str\] \= None

---

## **33\. Core Validation Function**

def validate\_decision\_rule\_for\_candidate(  
    entry: DecisionRegistryEntry,  
    action\_type\_ref: str,  
    agent\_type\_ref: str,  
    target\_type: str,  
) \-\> None:  
    if entry.status \!= DecisionStatus.ACTIVE:  
        raise InvalidDecisionRuleError(  
            f"Decision Rule is not active: {entry.decision\_rule\_id}"  
        )

    if action\_type\_ref not in entry.applicable\_action\_type\_refs:  
        raise DecisionActionTypeMismatchError(  
            f"Action Type '{action\_type\_ref}' is not applicable to "  
            f"Decision Rule '{entry.decision\_rule\_id}'"  
        )

    if agent\_type\_ref not in entry.applicable\_agent\_type\_refs:  
        raise DecisionAgentTypeMismatchError(  
            f"Agent Type '{agent\_type\_ref}' is not applicable to "  
            f"Decision Rule '{entry.decision\_rule\_id}'"  
        )

    if target\_type not in entry.applicable\_target\_types:  
        raise DecisionTargetTypeMismatchError(  
            f"Target Type '{target\_type}' is not applicable to "  
            f"Decision Rule '{entry.decision\_rule\_id}'"  
        )

    if not entry.required\_evidence\_refs:  
        raise InvalidDecisionRuleError(  
            "required\_evidence\_refs must be declared"  
        )

    if not entry.required\_policy\_refs:  
        raise InvalidDecisionRuleError(  
            "required\_policy\_refs must be declared"  
        )

    if not entry.decision\_criteria\_refs:  
        raise InvalidDecisionRuleError(  
            "decision\_criteria\_refs must be declared"  
        )

    if not entry.allowed\_decision\_outcomes:  
        raise InvalidDecisionRuleError(  
            "allowed\_decision\_outcomes must be declared"  
        )

    if entry.default\_decision\_outcome not in entry.allowed\_decision\_outcomes:  
        raise InvalidDecisionRuleError(  
            "default\_decision\_outcome must be included in allowed\_decision\_outcomes"  
        )

    if not entry.decision\_boundary:  
        raise InvalidDecisionRuleError(  
            "decision\_boundary must be declared"  
        )

    if not entry.approval\_boundary:  
        raise InvalidDecisionRuleError(  
            "approval\_boundary must be declared"  
        )

    if not entry.execution\_boundary:  
        raise InvalidDecisionRuleError(  
            "execution\_boundary must be declared"  
        )

    if not entry.safety\_boundary:  
        raise InvalidDecisionRuleError(  
            "safety\_boundary must be declared"  
        )

---

## **34\. Test Scenarios**

필수 테스트는 다음과 같다.

1\. 등록되지 않은 Decision Rule 거부  
2\. inactive Decision Rule 거부  
3\. deprecated Decision Rule 사용 거부  
4\. blocked Decision Rule 사용 거부  
5\. applicable Action Type 불일치 거부  
6\. applicable Agent Type 불일치 거부  
7\. target type 불일치 거부  
8\. required evidence 누락 거부  
9\. required policy reference 누락 거부  
10\. runtime snapshot requirement 누락 거부  
11\. decision criteria 누락 거부  
12\. allowed outcome 누락 거부  
13\. default outcome이 allowed outcome에 없을 경우 거부  
14\. decision boundary 누락 거부  
15\. approval boundary 누락 거부  
16\. execution boundary 누락 거부  
17\. safety boundary 누락 거부  
18\. DecisionCase 없이 ApprovalRequest 생성 불가 검증  
19\. Decision pass만으로 ApprovedAction 생성 불가 검증  
20\. Decision pass만으로 ExecutionRequest 생성 불가 검증

---

## **35\. Final Rule**

등록된 Decision Rule이 없으면,  
유효한 DecisionCase도 없다.

유효한 DecisionCase가 없으면,  
ApprovalRequest도 없다.

Decision Outcome이 proceed\_to\_approval이 아니면,  
ApprovalRequest로 이동할 수 없다.

Decision은 Approval이 아니다.

Decision은 Safety Gate가 아니다.

Decision은 ExecutionRequest가 아니다.

Decision은 Physical Command가 아니다.

Decision은 External System 제어가 아니다.

`decision_registry`는 LEDO Action Lifecycle에서 Agent의 제안과 Human Approval 사이를 통제하는 핵심 결정론적 판단 레지스트리이다.

이 모듈은 ActionCandidate가 임의로 승인 단계로 넘어가는 것을 방지하고, 모든 후보 Action이 evidence, policy, risk, priority, escalation, approval routing 기준을 통과하도록 보장한다.

핵심 정의는 다음과 같다.

Decision Registry  
\= 판단 결과 목록이 아니라,  
ActionCandidate가 DecisionCase로 전환되고  
ApprovalRequest로 라우팅되기 위한  
판단 기준, 증거 요구사항, 정책 요구사항,  
위험 분류, escalation, approval routing을 통제하는  
운영 계약 레지스트리

