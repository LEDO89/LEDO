# CLI Application

`apps/cli/` is the command-line interface for local deterministic operation.

It is intended for demos, event replay, registry inspection, contract export, validation, and audit inspection.

The CLI must call framework/runtime and must preserve validation, policy, Safety Gate, Evidence, and Audit boundaries.
