# Frontend Features

Feature folders are UI feature modules.

They are not ontology source-of-truth modules.

They may map to domain concepts visually, but semantic authority remains in ontology docs, domain_packs, registries, backend contracts, and backend validation.
