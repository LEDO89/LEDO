# Codex Task Prompt Template

Read `AGENTS.md` and the current module's `implementation_guide.md`.

Start by showing the file plan.

Implement only the declared MVP scope.

Do not invent domain-specific rules.

Use mocks, interfaces, and TODO placeholders when field knowledge is missing.
