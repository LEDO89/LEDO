# JSON Schema Contracts

This directory is for JSON Schema contracts derived from approved implementation models over time.

JSON Schema contracts help align API payloads, frontend validation, workers, adapters, and tests.
